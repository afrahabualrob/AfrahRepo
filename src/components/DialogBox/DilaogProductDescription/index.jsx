// import React, { useState } from "react";

// const index = ({description =" Build enlogic is written in their own state, then compose them to make complex UIs. Since component JavaScript instead of templates, you can easily passcapsulated components that manage their own their own state, then compose them to make complex UIs. Since componentstate,rich data through your app and keep state out of   then compose them to make complex UIs. Since component logic is written in JavaScriptinstead of templates, you can easily pass rich data through your app andkeep state out of the DOM. Build encapsulated components that manage"}) => {
  
//      const [dd, setDd] = useState(description.substring(1, 300));
//      const [open, setOpen] = useState(false);

//      const change = () => {
//        !open
//          ? setDd(description.substring(1))
//          : setDd(description.substring(1, 300));
//        setOpen(!open);
//      };
//      return (
//        <p>
//          {description.length < 300 ? (
//            <span>{description}</span>
//          ) : (
//            <div>
//              <span>{dd}</span>
//              <span onClick={change}>{!open ? "Show More" : "Show Less"}</span>
//            </div>
//          )}
//        </p>
//      );

// }

// export default index